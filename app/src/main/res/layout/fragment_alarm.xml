<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/android:windowBackground">

    <!-- Back Icon -->
    <ImageView
        android:id="@+id/iv_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_back"
        android:contentDescription="Back"
        android:padding="16dp"/>

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Medication Alarms"
        android:textSize="20sp"
        android:textColor="?attr/colorPrimary"/>

    <!-- RecyclerView for Existing Alarms -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_alarms"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="64dp"
        android:layout_marginHorizontal="16dp"
        android:layout_above="@id/ll_add_alarm_container"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>

    <!-- Add Alarm Container (Hidden by Default) -->
    <LinearLayout
        android:id="@+id/ll_add_alarm_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_alignParentBottom="true"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Select Medications"
            android:textColor="?attr/android:textColorPrimary"
            android:layout_marginBottom="8dp"/>

        <MultiAutoCompleteTextView
            android:id="@+id/multi_select_medications"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/surface_alternate"
            android:layout_marginBottom="16dp"
            android:inputType="textAutoComplete"
            android:completionThreshold="1"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Set Time"
            android:textColor="?attr/android:textColorPrimary"
            android:layout_marginBottom="8dp"/>

        <TextView
            android:id="@+id/tv_selected_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select Time"
            android:textColor="?attr/android:textColorPrimary"
            android:background="@drawable/btn_register"
            android:padding="12dp"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Repeat Pattern"
            android:textColor="?attr/android:textColorPrimary"
            android:layout_marginBottom="8dp"/>

        <Spinner
            android:id="@+id/spinner_repeat_pattern"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/surface_alternate"
            android:layout_marginBottom="16dp"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="16dp">

            <Button
                android:id="@+id/btn_cancel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Cancel"
                android:layout_marginEnd="8dp"
                style="?attr/materialButtonStyle"/>

            <Button
                android:id="@+id/btn_save_alarm"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Save Alarm"
                style="?attr/materialButtonStyle"/>

        </LinearLayout>
    </LinearLayout>

    <!-- Floating Action Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_alarm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:contentDescription="ADD NEW ALARM"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="@color/fab_background"
        app:tint="@color/fab_icon_color"/>

</RelativeLayout>